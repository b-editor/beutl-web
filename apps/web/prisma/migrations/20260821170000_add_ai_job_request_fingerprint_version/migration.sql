-- どの作り方で指紋を取ったジョブか。
--
-- モデルを名指ししない依頼を、以前は「そのとき解決された既定モデル入り」で
-- 指紋化していた。入れ替え配備の最中は両方の形が並ぶので、記録されているモデルで
-- 作り直した指紋とも突き合わせて、同じ依頼として認めている。
--
-- ただしそれだけでは行き過ぎる。現行の形で「モデル A を名指しした」と記録された
-- ジョブは、モデルを名乗らない依頼と作り直した指紋が一致してしまい、別の依頼に
-- A の結果を返すことになる。この列があるジョブは現行の形——記録されたとおりの
-- 指紋としか一致しない。NULL は旧版が書いたジョブで、そちらだけが作り直しの
-- 対象になる。
--
-- 旧版のジョブが残らなくなれば、この列ごと突き合わせの分岐を落とせる。
ALTER TABLE "AiJob" SET (schema_locked = false);

ALTER TABLE "AiJob" ADD COLUMN "requestFingerprintVersion" INT4;

ALTER TABLE "AiJob" SET (schema_locked = true);
