"use client"

import { useEffect } from "react"

export default async function Page({
  returnUrl, provider
}: { returnUrl: string, provider: string }) {
  useEffect(()=>{

  }, []);

  return (<></>)
}
